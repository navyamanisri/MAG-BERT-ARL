"""
MAG-BERT-ARL API Module

This package houses the REST API layer of the backend application.
It exposes endpoints for video ingestion, processing statuses, evaluation metrics,
and demographic fairness auditing reports.

Typically contains:
- routes/: Endpoint handlers grouped by feature (e.g., uploads, analytics).
- schemas/: Pydantic models defining input validation and serialization structures.
- dependencies/: FastAPI dependency injection helper functions.
"""
